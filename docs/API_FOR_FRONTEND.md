# คู่มือเรียกใช้ API สำหรับคนทำหน้าเว็บ

ระบบหลังบ้านเปิดใช้งานจริงแล้ว ต่อได้เลยไม่ต้องติดตั้งอะไรในเครื่อง

**ที่อยู่**: `http://161.118.237.37/api`

ตัวอย่างทั้งหมดในเอกสารนี้เป็นคำขอและคำตอบที่ยิงกับเซิร์ฟเวอร์จริง ไม่ใช่ตัวอย่างสมมติ

---

## กฎสองข้อที่ต้องรู้ก่อนเขียนโค้ด

**1. เรียกด้วย path สัมพัทธ์ `/api/*` เสมอ อย่าใส่ที่อยู่เต็ม**

```js
fetch("/api/match", ...)                      // ถูก
fetch("http://161.118.237.37/api/match", ...) // ผิด
```

ถ้าใส่ที่อยู่เต็มจะติด CORS และเวลาย้ายเซิร์ฟเวอร์ต้องแก้โค้ดทุกจุด การใช้ path สัมพัทธ์
ทำให้เบราว์เซอร์เห็นเป็น origin เดียวกับหน้าเว็บ

**2. ทุก endpoint ยกเว้น `/courses` ต้องแนบ token**

---

## ขั้นที่ 1 — สมัครสมาชิกและเข้าสู่ระบบ

```
POST /api/auth/register     { "email": "...", "password": "..." }   รหัสผ่านอย่างน้อย 8 ตัว
POST /api/auth/login        { "email": "...", "password": "..." }
```

`/auth/login` คืน

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6...",
  "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6...",
  "token_type": "bearer"
}
```

เก็บ `access_token` ไว้แล้วแนบไปกับทุกคำขอ

```js
headers: { Authorization: `Bearer ${accessToken}` }
```

> **ข้อควรระวัง** token หมดอายุใน 30 นาที และตอนนี้**ยังไม่มี endpoint ต่ออายุ**
> ถ้าเจอ 401 ให้พาผู้ใช้ไปเข้าสู่ระบบใหม่ ถ้าต้องการให้ต่ออายุอัตโนมัติบอกได้ เดี๋ยวเพิ่มให้

---

## ขั้นที่ 2 — ค้นหาสาขาที่เหมาะกับคุณ

```
POST /api/match
```

ส่งคำตอบทั้ง 5 ขั้นมาในคำขอเดียว **ไม่ต้องบันทึกโปรไฟล์ก่อน** และไม่ต้องตอบครบทุกช่อง

| ช่อง | ชนิด | ตรงกับขั้นตอนไหน |
|---|---|---|
| `study_track` | ข้อความ | ขั้น 1 สายการเรียน |
| `favorite_subjects` | รายการข้อความ | ขั้น 1 วิชาที่ชอบ |
| `interests` | รายการข้อความ | ขั้น 2 ความสนใจ |
| `aptitudes` | รายการข้อความ | ขั้น 3 ความถนัด |
| `career_goal` | รายการข้อความ | ขั้น 4 เป้าหมายอาชีพ |
| `work_environment` | รายการข้อความ | ขั้น 4 สภาพแวดล้อมการทำงาน |
| `extra` | ข้อความ | ช่องพิมพ์อิสระ (ถ้ามี) |
| `limit` | ตัวเลข 1–10 | จำนวนผลลัพธ์ ค่าเริ่มต้น 5 |

**ส่งข้อความตามที่แสดงบนปุ่มได้เลย** ไม่ต้องแปลงเป็นรหัส ระบบเข้าใจภาษาไทยธรรมดา

```js
const res = await fetch("/api/match", {
  method: "POST",
  headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}` },
  body: JSON.stringify({
    study_track: "วิทย์-คณิต",
    favorite_subjects: ["คอมพิวเตอร์", "คณิตศาสตร์"],
    interests: ["เทคโนโลยีและคอมพิวเตอร์"],
    aptitudes: ["ชอบเขียนโค้ด พัฒนาโปรแกรม และแก้บั๊ก"],
    career_goal: ["ทำงานในองค์กรขนาดใหญ่"],
    work_environment: ["ทำงานในออฟฟิศ"],
    limit: 4,
  }),
});
const data = await res.json();
```

**คำตอบที่ได้จริง** (ตัดให้เหลือ 2 รายการเพื่อความสั้น)

```json
{
  "profile_text": "เรียนสายวิทย์-คณิต ชอบวิชา คอมพิวเตอร์, คณิตศาสตร์ สนใจด้าน เทคโนโลยีและคอมพิวเตอร์ ถนัด ชอบเขียนโค้ด พัฒนาโปรแกรม และแก้บั๊ก เป้าหมายอาชีพคือ ทำงานในองค์กรขนาดใหญ่ อยากทำงานแบบ ทำงานในออฟฟิศ",
  "model_used": "gemini-3.5-flash-lite",
  "confidence": "low",
  "matches": [
    {
      "course_id": "34c21266-4e5a-407c-8e77-fc95d7c94fe1",
      "title": "หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาวิทยาการคอมพิวเตอร์ (พ.ศ. 2561)",
      "score": 0.5545,
      "match_percent": 55,
      "rationale": "หลักสูตรนี้มีความสอดคล้องกับความสนใจด้านเทคโนโลยีและคอมพิวเตอร์ รวมถึงเป้าหมายอาชีพนักเขียนโปรแกรมที่ตรงกับสิ่งที่นักเรียนมีความถนัดในการเขียนโค้ดและพัฒนาโปรแกรม",
      "evidence_chunk_ids": ["b6950dd5-d8ae-4f2c-97b1-db0a84e3ada2"]
    },
    {
      "course_id": "2fa6627f-e2fa-4956-b9d0-d0c82ad3dcaf",
      "title": "หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาเทคโนโลยีสารสนเทศ (พ.ศ. 2566)",
      "score": 0.5448,
      "match_percent": 53,
      "rationale": "ข้อมูลในหลักสูตรระบุถึงความต้องการพัฒนาทักษะด้านการเขียนโปรแกรมและการพัฒนาซอฟต์แวร์ ซึ่งตรงกับความถนัดและความชอบในการเขียนโค้ดและพัฒนาโปรแกรมของนักเรียน"
    }
  ]
}
```

**`confidence` — ใช้เปลี่ยนวิธีแสดงผล**

คำตอบมีฟิลด์ `confidence` เป็น `"high"` หรือ `"low"` บอกว่าควรเชื่ออันดับ 1 แค่ไหน

| ค่า | หมายถึง | ควรแสดงอย่างไร |
|---|---|---|
| `high` | อันดับ 1 ทิ้งห่างชัดเจน | ชูอันดับ 1 เป็นคำตอบหลักได้ |
| `low` | ผลลัพธ์เกาะกลุ่มกัน | ขึ้นข้อความชวนให้ดูหลายสาขา เช่น *"ผลลัพธ์ใกล้เคียงกันมาก แนะนำให้ดูหลายสาขาประกอบกัน"* |

**ที่มาของค่านี้ไม่ใช่การเดา** วัดจากชุดทดสอบ 25 โปรไฟล์ — เมื่อระบบตอบถูก อันดับ 1
ทิ้งห่างอันดับ 3 เฉลี่ย 13.9 จุด แต่เมื่อตอบผิดห่างเพียง 9.6 จุด **ผลที่เกาะกลุ่มกันจึงเป็น
สัญญาณเตือนว่าอันดับ 1 อาจไม่ใช่คำตอบที่ผู้ใช้ต้องการ**

ตัวอย่างจริงสองแบบ

```
โปรไฟล์ "ชอบแต่งรูปทำคลิป แต่ก็ลองเขียนเว็บบ้าง ยังไม่รู้ว่าเก่งอันไหน"
   confidence = low    55% / 54% / 54%   ห่าง 1 จุด

โปรไฟล์ "สนใจการพัฒนาผลิตภัณฑ์บำรุงผิวและการทดสอบคุณภาพ"
   confidence = high   71% / 50% / 39%   ห่าง 32 จุด
```

**เอาไปแสดงยังไง**

| ฟิลด์ | ใช้ทำอะไร |
|---|---|
| `title` | ชื่อบนการ์ด — ตัด `หลักสูตร...สาขาวิชา` ออกได้ถ้าอยากให้สั้น |
| `match_percent` | ตัวเลข % และความยาวแถบ |
| `rationale` | ข้อความอธิบายใต้ชื่อ |
| `course_id` | ใช้กับปุ่ม "ดูรายละเอียด" → `GET /api/courses/{course_id}` |
| `score` | ค่าดิบ ไม่ต้องแสดง เก็บไว้ตรวจสอบ |
| `profile_text` | ข้อความที่ระบบเข้าใจ มีประโยชน์ตอนดีบัก |
| `confidence` | ใช้ตัดสินว่าจะชูอันดับ 1 หรือชวนให้ดูหลายตัวเลือก |

> **`match_percent` คืออะไร** เป็นความใกล้เคียงเชิงความหมายระหว่างคำตอบของนักเรียน
> กับเนื้อหาและชื่อหลักสูตร คำนวณจากเวกเตอร์จริงและถามซ้ำได้ค่าเดิมเสมอ
>
> **ไม่ใช่** ความน่าจะเป็นที่จะเรียนจบหรือคะแนนความเหมาะสมที่พิสูจน์แล้ว
> ควรมีข้อความกำกับใต้ตัวเลขสักบรรทัด เช่น *"คำนวณจากความสอดคล้องระหว่างคำตอบของคุณ
> กับเนื้อหาหลักสูตร"* เพราะตัวเลขเปอร์เซ็นต์ทำให้คนเข้าใจว่าแม่นกว่าความเป็นจริง

**ความแม่นที่วัดได้** ทดสอบด้วยโปรไฟล์ 25 ชุดที่กำหนดคำตอบไว้ล่วงหน้า ครอบคลุมครบทุกสาขา

| ตัวชี้วัด | ผล |
|---|---|
| สาขาที่ควรได้ อยู่อันดับ 1 | 17/25 |
| สาขาที่ควรได้ ติด 3 อันดับแรก | **24/25** |

**ตัวเลขนี้คือเหตุผลที่หน้าเว็บควรแสดงหลายรายการ** อันดับ 1 อย่างเดียวถูกราว 68% แต่พอ
ดู 3 อันดับแรกขึ้นเป็น 96% การให้ผู้ใช้เลือกเองจากหลายตัวเลือกจึงแม่นกว่ามาก — แบบร่างที่
แสดง 4 รายการตรงกับความสามารถจริงของระบบพอดีแล้ว

---

## ขั้นที่ 3 — รายการหลักสูตร

```
GET /api/courses               ไม่ต้องใช้ token
GET /api/courses/{course_id}   ไม่ต้องใช้ token
```

ใช้กับหน้า "หลักสูตรทั้งหมด" และปุ่มดูรายละเอียดจากหน้าผลลัพธ์

**ระวัง: มี 18 รายการ แต่มีแค่ 14 สาขา** เพราะบางสาขามีเอกสารสองฉบับคือฉบับเก่ากับ
ฉบับปรับปรุง ชื่อจะลงท้ายด้วยปีต่างกัน

```
หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาวิทยาการคอมพิวเตอร์ (พ.ศ. 2561)
หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาวิทยาการคอมพิวเตอร์ (พ.ศ. 2566)
```

หน้า "หลักสูตรทั้งหมด" ควรแสดง**ฉบับปีล่าสุดของแต่ละสาขา**เท่านั้น ไม่งั้นผู้ใช้จะ
เห็นสาขาซ้ำแล้วเผลอเอาสองฉบับของสาขาเดียวกันไปเปรียบเทียบกันเอง

---

## ขั้นที่ 4 — แชทกับที่ปรึกษา

มีสองแบบ **หน้าเว็บควรใช้แบบสตรีม**

```
POST /api/chat          รอจนเขียนเสร็จแล้วส่งทีเดียว
POST /api/chat/stream   ทยอยส่งทีละส่วน
```

ทั้งคู่รับ `{ "message": "...", "session_id": null }` ส่ง `session_id` ที่ได้กลับมาในครั้งถัดไป
เพื่อคุยต่อในบทสนทนาเดิม

**อย่าใช้ `await response.json()` กับ `/chat/stream`** เพราะคำตอบทยอยมาทีละส่วน ถ้ารอ
ทั้งก้อนจะเสียข้อดีไปหมด — โค้ดที่ใช้ได้จริงอยู่ในฟังก์ชัน `chatStream` ที่
`frontend/lib/api.ts` ลอกไปใช้ได้เลย

รูปแบบข้อมูลที่ทยอยมา (Server-Sent Events)

```
event: meta      ข้อมูลสรุป — session_id, status, รายการอ้างอิง  (มาก่อนตัวอักษรแรก)
event: token     ข้อความส่วนถัดไป {"t":"..."}                     (มาหลายครั้ง)
event: done      จบแล้ว
event: error     เกิดข้อผิดพลาด {"detail":"..."}
```

`status` ใน `meta` บอกว่าระบบตัดสินอย่างไร ใช้ตกแต่งหน้าจอได้

| ค่า | ความหมาย | ควรแสดงอย่างไร |
|---|---|---|
| `answered` | ตอบจากเอกสารจริง | แสดงปุ่มดูรายการอ้างอิง |
| `not_found` | อยู่ในเรื่องแต่เอกสารไม่มีข้อมูล | ไม่ต้องแสดงรายการอ้างอิง |
| `out_of_scope` | นอกขอบเขต | ไม่ต้องแสดงรายการอ้างอิง |
| `small_talk` | ทักทาย | ไม่ต้องแสดงรายการอ้างอิง |

---

## ขั้นที่ 5 — เปรียบเทียบสาขา

```
POST /api/compare   { "course_ids": ["<id>", "<id>"] }   เลือกได้ 2 ถึง 4 สาขา
```

เอา `course_ids` มาจาก `GET /api/courses` (ขั้นที่ 3)

```json
{
  "programs": [
    { "course_id": "34c21266-...", "title": "หลักสูตรวิทยาศาสตรบัณฑิต สาขาวิชาวิทยาการคอมพิวเตอร์ (พ.ศ. 2566)" }
  ],
  "rows": [
    { "dimension": "ค่าเทอม",        "values": ["ภาคปกติ 12,000 บาท · ภาคเสาร์-อาทิตย์ 14,000 บาท"] },
    { "dimension": "คำอธิบายสาขา",   "values": ["..."] },
    { "dimension": "ระยะเวลาเรียน",  "values": ["..."] },
    { "dimension": "จำนวนหน่วยกิต",  "values": ["..."] },
    { "dimension": "เรียนอะไรบ้าง",   "values": ["..."] },
    { "dimension": "ทักษะที่ได้รับ",   "values": ["..."] },
    { "dimension": "จบแล้วทำงานอะไร", "values": ["..."] },
    { "dimension": "คุณสมบัติผู้เข้าศึกษา", "values": ["..."] }
  ],
  "summary": "สรุปความต่างเป็นภาษาคน"
}
```

`values` เรียงตรงกับลำดับใน `programs` เสมอ จับคู่คอลัมน์ด้วยตำแหน่งได้เลย

ช่องที่เอกสารไม่มีข้อมูลจะได้ข้อความ **"ไม่พบข้อมูลในเอกสาร"** ไม่ใช่ค่าว่าง — แสดงตรงๆ
ได้ ไม่ต้องซ่อนแถว

ทุกแถวยกเว้น **ค่าเทอม** สรุปมาจากเอกสาร มคอ.2 ของสาขานั้นๆ ส่วนค่าเทอมมาจากประกาศ
ของคณะซึ่งเป็นคนละแหล่ง

**ใช้เวลาราว 7-8 วินาทีสำหรับสองสาขา** และนานขึ้นตามจำนวนสาขาที่เลือก เพราะสรุปทีละ
สาขาแยกกัน ต้องมีตัวโหลดระหว่างรอ

---

## ขั้นที่ 6 — ประวัติการสนทนา (แถบข้างในหน้าแชท)

```
GET /api/chat/sessions?limit=20&offset=0   รายการบทสนทนาเก่า
GET /api/chat/sessions/{session_id}        ข้อความทั้งหมดในบทสนทนานั้น
```

ทั้งคู่ต้องแนบ token เหมือน endpoint อื่น และเห็นเฉพาะบทสนทนาของตัวเองเท่านั้น

**รายการบทสนทนา** เรียงจากที่คุยล่าสุดก่อน

```json
{
  "sessions": [
    {
      "session_id": "90fe4c23-082c-4eb0-9684-40d841bc31be",
      "title": "ค่าเทอมวิทยาการคอมพิวเตอร์เท่าไหร่",
      "started_at": "2026-08-29T11:16:59.095470Z",
      "last_message_at": "2026-08-29T11:16:59.098542Z",
      "message_count": 2
    }
  ],
  "total": 2,
  "has_more": false
}
```

`title` **ย่อมาจากคำถามแรกของผู้ใช้** ไม่ได้ให้ AI ตั้งชื่อ ตัดมาไม่เกิน 60 ตัวอักษร
แล้วต่อท้ายด้วย `…` ถ้ายาวกว่านั้น เอาไปแสดงได้ตรงๆ

`has_more` บอกว่าควรแสดงปุ่ม **"ดูเพิ่มเติม"** หรือไม่ กดแล้วยิงซ้ำโดยเพิ่ม `offset`
ไม่ต้องคำนวณจาก `total` เอง

`last_message_at` ใช้จัดกลุ่ม **"วันนี้ / เมื่อวาน / ก่อนหน้านี้"** ในแถบข้าง

**ข้อความในบทสนทนา** เรียงจากเก่าไปใหม่ พร้อมแสดงต่อกันได้เลย

```json
{
  "session_id": "53dd8cb6-3c50-4354-95ee-cd822bdb6779",
  "title": "เรียน IT ต้องเก่งคณิตศาสตร์ไหม",
  "started_at": "2026-08-29T11:16:53.438326Z",
  "messages": [
    { "role": "user",      "content": "เรียน IT ต้องเก่งคณิตศาสตร์ไหม", "created_at": "..." },
    { "role": "assistant", "content": "...",                          "created_at": "..." }
  ]
}
```

**คำถามกับคำตอบมี `created_at` เท่ากัน** เพราะถูกบันทึกพร้อมกัน ให้แสดงตามลำดับที่
ส่งมาในอาร์เรย์ **อย่าเรียงใหม่ตามเวลา** เพราะจะได้ลำดับที่ไม่แน่นอน

สามอย่างที่ต้องทำในหน้าแชท

| ปุ่ม | ต้องทำอะไร |
|---|---|
| New chat | ล้างหน้าจอ แล้วส่ง `session_id: null` ในคำถามถัดไป |
| กดรายการในแถบข้าง | `GET /api/chat/sessions/{id}` แล้วส่ง id นั้นเป็น `session_id` ตอนถามต่อ |
| ดูเพิ่มเติม | ยิง `/api/chat/sessions` ซ้ำโดยเพิ่ม `offset` |

บทสนทนาที่ยังไม่มีข้อความจะไม่ขึ้นในรายการเลย ไม่ต้องกรองเอง

---

## สิ่งที่ยังไม่มี บอกได้ถ้าต้องการ

| เมนูในแบบร่าง | สถานะ |
|---|---|
| ลบบทสนทนา | ยังไม่มี endpoint — บอกได้ถ้าแบบร่างต้องการ |
| อาชีพหลังจบ (หน้าแยก) | ยังไม่มี endpoint เฉพาะ ตอนนี้ใช้แถว "จบแล้วทำงานอะไร" จาก `/api/compare` แทนได้ |
| แนบไฟล์ในหน้าแชท | ไม่รองรับ และไม่ได้ออกแบบให้รองรับ — ระบบตอบจากเอกสารหลักสูตรที่เตรียมไว้เท่านั้น |
| พูดใส่ไมค์ | ทำได้ฝั่งหน้าเว็บล้วน (Web Speech API ของเบราว์เซอร์) ไม่ต้องแตะหลังบ้าน |
| ต่ออายุ token อัตโนมัติ | ยังไม่มี |

---

## ลองยิงดูก่อนเขียนโค้ด

เปิด `http://161.118.237.37/api/docs` จะได้หน้าเอกสารอัตโนมัติที่กดทดลองยิงได้ทุก endpoint
เห็นรูปแบบข้อมูลครบทุกช่องโดยไม่ต้องเขียนโค้ดสักบรรทัด
